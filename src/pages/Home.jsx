import React from 'react';
import Hero from '../components/Hero';

const Home = () => {
  return (
    <main>
      <Hero />
      {/* Add featured properties, testimonials, etc. here */}
    </main>
  );
};

export default Home;
